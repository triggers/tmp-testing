#!/bin/bash

cat <<'EOF'
HINT:

Before ssh will respond, the instance's image must be expanded, then
the VM boots, and finally the ssh server must start.  It should take
about 30 seconds but may take longer.
EOF
